[project]
name = "NVDAComputerUse"
version = "2026.1.0"
description = "NVDA Add-on for OpenAI Computer Use"
readme = "readme.md"
requires-python = ">=3.7"
dependencies = [
    "markdown>=3.0",
    "scons>=4.0",
]

[tool.uv]
managed = true
